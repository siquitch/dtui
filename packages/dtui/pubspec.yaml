name: dtui
description: >-
  A zero-dependency terminal UI framework for Dart with composable widgets,
  flex/fixed layout engine, and efficient diff-based ANSI rendering.
version: 0.1.0
topics: [tui, terminal, cli, widgets]

environment:
  sdk: ^3.5.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
